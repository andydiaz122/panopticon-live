interface:
  display_name: "Panopticon Live"
  short_description: "Repo-specific patterns and workflows for panopticon-live"
  default_prompt: "Use the panopticon-live repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true